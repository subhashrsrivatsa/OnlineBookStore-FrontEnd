export class Book {

    // same fields as that of the SpringBoot Book class

    sku: string;
    name: string;
    description: string;
    unitPrice: number;
    imageURL: string;
    active: boolean;
    unitsInStock: number;
    createdOn: Date;
    updatedOn: Date;
}
